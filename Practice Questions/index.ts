// @ts-ignore

// 1) Declare a variable with a number type and assign it a value. Log the variable to the console.

var num:number = 44
console.log(num)



// 2) Create a function that takes a string as an argument and returns its length. Use type annotations.

function giveString(str:string):number{
    return str.length
}
console.log(giveString("Hello"))


// Declare an array of numbers and initialize it with some values. Log the array to the console.

var arrayOfNum:number[]=[4,4,4,4,4]
console.log(arrayOfNum)
